public class DynamicClassificacaoProvider implements ClassificationProvider {
    @Override
    public int getClassificacao(Tape tape) {
        int type = 0;
        // Implementação que retorna a classificação dinâmica
        return type;
    }
}